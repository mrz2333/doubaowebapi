# doubaowebapi 环境变量配置
# 复制此文件为 .env 并按需修改

# 服务端口
DOUBAO_PORT=8458

# 监听地址 (Docker 部署用 0.0.0.0)
DOUBAO_HOST=127.0.0.1

# API 密钥 (用于保护 /v1/* 端点，留空则不鉴权；同时也是 /admin 登录密码)
DOUBAO_API_KEY=

# Session 文件路径
DOUBAO_SESSION_FILE=.doubao_session.json

# 每分钟请求限制
DOUBAO_RPM_LIMIT=50

# 请求超时 (秒)
DOUBAO_TIMEOUT=180

# Session 保活间隔 (秒，默认 2 小时)
DOUBAO_KEEPALIVE_INTERVAL=7200

# 默认 Bot ID (7234781073513644036 = 默认, 7338286299411103781 = 扩展版/Expert)
DOUBAO_BOT_ID=7234781073513644036

# msToken (可选，留空最安全；假值会触发风控)
DOUBAO_MS_TOKEN=

# 连接外部 Chromium CDP (可选，如 VNC 浏览器栈)
# DOUBAO_CDP_URL=http://127.0.0.1:9222

# 浏览器持久化数据目录 (独立模式用)
DOUBAO_BROWSER_DATA=/app/data/.doubao_browser

# Headless 模式 (CDP 模式下忽略此项)
DOUBAO_HEADLESS=true

# noVNC 远程桌面地址 (可选，用于手动风控验证)
# DOUBAO_NOVNC_URL=http://127.0.0.1:6080
